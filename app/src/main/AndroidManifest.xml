<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crishna.contactsbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactsBook">
        <activity
            android:name=".view.update.EditContactActivity"
            android:exported="true" />
        <activity
            android:name=".view.create.CreateContactActivity"
            android:exported="true" />
        <activity
            android:name=".view.info.ContactInfoActivity"
            android:exported="true"></activity>
        <activity
            android:name=".view.home.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>